/** @type {import('next').NextConfig} */
const nextConfig = {
  // Serve the static index.html from pages/ as the root page via a passthrough
  // (the actual serving is done by pages/index.js below)
};
module.exports = nextConfig;
